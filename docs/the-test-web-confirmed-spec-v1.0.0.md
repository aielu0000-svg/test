# ザ・テスト Web版 確定仕様書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | ザ・テスト Web版 確定仕様書 |
| 仕様バージョン | 1.0.0 |
| 確定日 | 2026-07-28 |
| 対象 | OpenShift上で稼働するWebアプリケーション |
| 既存製品 | Electron 29 / React 18 / TypeScript / SQLiteによるWindowsデスクトップアプリ |
| 本書の位置付け | Web版の設計・実装・受け入れ試験の基準 |
| 主な利用者 | 管理者、実行者 |
| データベース | MariaDB |
| 証跡ストレージ | PVC |
| 認証方式 | アプリ内ユーザー名・パスワード認証 |

本書は、現行デスクトップ版の機能を基礎とし、Web版について確定した仕様を統合したものである。
旧仕様にOpenShift OAuth、PostgreSQL、4種類のロールなどの記述がある場合、本書の内容を優先する。

---

## 2. システムの目的

複数のプロジェクトで共通利用できるテスト管理Webアプリケーションを提供する。

プロジェクトごとに、次の情報を独立して管理する。

1. テストケース
2. テストケースフォルダ
3. シナリオ
4. 初期データ
5. Markdown手順書
6. テスト実行
7. ケース・シナリオ単位の実行結果
8. 証跡ファイル
9. インポート・エクスポート
10. 削除データと復元
11. 操作監査ログ

1人のユーザーが複数のプロジェクトへ参加できる。
プロジェクトに割り当てられていないユーザーも、全プロジェクトを閲覧およびエクスポートできる。

---

## 3. 開発方針

### 3.1 基本方針

Web版の基盤は新規に構築し、現行アプリから再利用可能な画面・業務ロジックだけを選択的に移植する。

現行Electronアプリをそのままコンテナ化する方式は採用しない。

### 3.2 再利用候補

- Reactコンポーネント
- TypeScriptの型定義
- 画面レイアウト
- Markdown手順書パーサー
- Markdown表示処理
- ケース・シナリオ画面
- Canvasによる画像編集処理
- 予定・実績日時の計算処理
- テスト状態・合格率の集計処理
- CSV、JSON、Markdown変換処理のうち仕様と一致する部分
- 共通UIコンポーネント

### 3.3 新規構築する部分

- Web API
- MariaDBスキーマ
- DBマイグレーション
- アプリ内認証
- プロジェクト権限
- 監査ログ
- 楽観ロック
- 実行スナップショット
- スナップショットのリビジョン管理
- 論理削除・復元
- 証跡ストレージ
- Excelインポート
- OpenShiftリソース
- バックアップ・復元処理
- SQLiteからのデータ移行ツール

---

## 4. 論理構成

```text
Browser
  │ HTTPS
  ▼
React Web UI
  │ HTTPS / JSON API
  ▼
Node.js / TypeScript API
  ├─ MariaDB
  ├─ 証跡用PVC
  ├─ 一時ファイル用領域
  ├─ バックアップ用ストレージ
  └─ バックグラウンド処理
```

### 4.1 推奨技術

#### フロントエンド

- React
- TypeScript
- Vite
- Tailwind CSS
- TanStack Query
- TanStack Table
- Markdownレンダラー
- HTMLサニタイザー
- Playwright

#### バックエンド

- Node.js
- TypeScript
- Fastify
- OpenAPI
- リクエスト・レスポンスのスキーマバリデーション
- MariaDBドライバー
- SQLマイグレーション

#### Excel

- ExcelJS
- `.xlsx`のみを正式対応形式とする

特定ライブラリに重大な互換性・保守性の問題が判明した場合は、同等機能を持つ無償のライブラリへ置き換えてよい。

---

## 5. プロジェクト仕様

### 5.1 基本仕様

- 1つのWebアプリで複数プロジェクトを管理する。
- すべての業務データは1つのプロジェクトに所属する。
- 1ユーザーは複数プロジェクトへ割り当て可能とする。
- プロジェクト間でテストケース、シナリオ、実行結果、証跡を混在させない。
- APIは画面表示上の制御だけに依存せず、すべての更新要求で`project_id`とユーザー権限を検証する。
- プロジェクトは`active`または`archived`の状態を持つ。
- プロジェクト作成、ユーザー割当、プロジェクト物理削除は管理者専用とする。

### 5.2 主なプロジェクト項目

- ID
- プロジェクト名
- 説明
- 状態
- 作成者
- 作成日時
- 更新日時
- 論理削除日時
- 論理削除者
- バージョン番号

---

## 6. ユーザー認証

### 6.1 認証方式

- OpenShift OAuthは使用しない。
- アプリケーション独自のユーザー名・パスワード認証とする。
- 全画面をログイン必須とする。
- 一般ユーザーの自己登録機能は設けない。
- ユーザーは管理者が作成する。
- 初期管理者は初回起動用設定または環境変数から作成する。
- ユーザーは物理削除せず、無効化する。
- 無効化後も過去の操作履歴は保持する。

### 6.2 ユーザー名

| 項目 | 仕様 |
|---|---|
| 入力方法 | 管理者による自由入力 |
| 必須 | 必須 |
| 最大長 | 100文字 |
| 重複 | 不可 |
| 大文字・小文字 | 区別しない |
| 前後空白 | 登録時に除去 |
| 変更 | 管理者が変更可能 |
| 無効化後の再利用 | 原則禁止 |
| 表示名 | ユーザー名とは別に任意設定可能 |

使用可能文字は、日本語、半角・全角英数字、通常の空白、ハイフン、アンダースコア、ピリオド、`@`とする。
制御文字、改行、タブは使用不可とする。

比較用の正規化値と表示用の入力値を分離して保存する。

### 6.3 パスワード

| 項目 | 仕様 |
|---|---|
| 最低文字数 | 4文字 |
| 最大文字数 | 128文字 |
| 文字種類の強制 | なし |
| 脆弱なパスワードの禁止 | なし |
| ユーザー名と同一 | 許可 |
| 過去パスワードの再利用 | 許可 |
| 有効期限 | なし |
| 日本語・全角文字 | 許可 |
| 初回ログイン | 仮パスワードからの変更を必須とする |
| 管理者による再設定 | 可能 |
| 確認入力 | 作成・変更時に必須 |

パスワードの前後空白は登録時およびログイン時に除去する。

### 6.4 パスワード保存

- 平文保存しない。
- Argon2idでハッシュ化する。
- ユーザーごとに異なるsaltを使用する。
- ログおよび監査ログへパスワードを出力しない。
- 管理者も現在のパスワードを閲覧できない。
- 再設定時は新しい仮パスワードを登録する。

### 6.5 ログイン保護

- 同一ユーザーで5回連続失敗した場合、5分間ログインを停止する。
- ログイン成功時に失敗回数をリセットする。
- 管理者はロックを解除できる。
- IPアドレス単位でも短時間の連続試行を制限する。
- ログイン成功・失敗を監査ログへ記録する。
- エラー文言は次で統一する。

```text
ユーザー名またはパスワードが正しくありません。
```

### 6.6 セッション

- Cookieベースのセッションを使用する。
- Cookieには`HttpOnly`を設定する。
- HTTPS環境では`Secure`を設定する。
- `SameSite`を設定する。
- ログアウト機能を設ける。
- パスワード変更時は、現在のセッション以外を無効化する。

---

## 7. ロールと権限

### 7.1 ロール

ロールは次の2種類とする。

- `admin`：管理者
- `executor`：実行者

### 7.2 権限モデル

管理者は全プロジェクトに対する全権限を持つ。

実行者は次の権限を持つ。

- 割当済みプロジェクト：業務データに対する全操作
- 未割当プロジェクト：閲覧およびエクスポートのみ

### 7.3 権限表

| 操作 | 管理者 | 割当済み実行者 | 未割当実行者 |
|---|---:|---:|---:|
| プロジェクト一覧・閲覧 | 可 | 可 | 可 |
| エクスポート | 可 | 可 | 可 |
| プロジェクト作成 | 可 | 不可 | 不可 |
| プロジェクト情報編集 | 可 | 可 | 不可 |
| ユーザー作成・変更・無効化 | 可 | 不可 | 不可 |
| プロジェクトへのユーザー割当 | 可 | 不可 | 不可 |
| ケース・シナリオ編集 | 可 | 可 | 不可 |
| データセット編集 | 可 | 可 | 不可 |
| 手順書編集 | 可 | 可 | 不可 |
| テスト実行作成・変更 | 可 | 可 | 不可 |
| 完了済み実行の変更 | 可 | 可 | 不可 |
| 証跡追加・編集・削除 | 可 | 可 | 不可 |
| インポート | 可 | 可 | 不可 |
| 業務データ削除・復元 | 可 | 可 | 不可 |
| プロジェクトアーカイブ | 可 | 不可 | 不可 |
| プロジェクト物理削除 | 可 | 不可 | 不可 |
| バックアップ・復元 | 可 | 不可 | 不可 |
| 全監査ログ閲覧 | 可 | 不可 | 不可 |

割当済み実行者は、担当プロジェクト内の監査履歴を画面上で確認できてもよいが、監査ログそのものの変更・削除はできない。

---

## 8. MariaDB

### 8.1 採用方針

正本データベースとしてMariaDBを採用する。

採用理由は次のとおり。

- 業務での運用実績がある。
- A5:SQL Mk-2で管理できる。
- 現行機能で必要なトランザクション、外部キー、検索、論理削除、監査、楽観ロックを実現できる。
- PostgreSQL固有機能を必須とする要件が現時点でない。
- 学習・運用コストを抑えられる。
- 無償で利用できる。

### 8.2 運用方針

- MariaDBには専用PVCを割り当てる。
- DBは外部ネットワークへ常時公開しない。
- A5:SQL Mk-2からの接続は、ポートフォワード等の管理用経路を利用する。
- DBスキーマ変更はマイグレーションファイルで管理する。
- 本番DBへ直接行った手動変更を正規の変更手段としない。
- 日時はUTCで保存し、画面では日本時間で表示する。
- 文字コードは`utf8mb4`とする。
- 外部キーを使用する。
- 業務データの削除に安易な`ON DELETE CASCADE`を使用しない。
- 一覧検索に必要なインデックスを設計する。

### 8.3 ID

- 主キーはUUIDを基本とする。
- MariaDBでは、運用・検索性を考慮し、文字列形式またはバイナリ形式を実装時に統一する。
- 外部公開用IDと内部連番を分ける方式も許可する。
- 方式は全テーブルで一貫させる。

---

## 9. ストレージと容量

### 9.1 初期容量

| 用途 | 初期容量 |
|---|---:|
| MariaDB | 20GiB |
| 証跡ファイル | 100GiB |
| インポート・エクスポート一時領域 | 10GiB |
| バックアップ | 実使用量を基準に3世代分 |

OpenShiftのストレージ機能で容量拡張可能な構成とする。

### 9.2 証跡ストレージ

初期実装ではPVCを使用する。

アプリケーションはストレージ処理を抽象化し、将来S3互換オブジェクトストレージへ移行可能な構成にする。

```ts
interface EvidenceStorage {
  save(...): Promise<StoredFile>;
  read(...): Promise<ReadableStream>;
  delete(...): Promise<void>;
  copyVersion(...): Promise<StoredFile>;
  createThumbnail(...): Promise<StoredFile>;
}
```

### 9.3 複数Pod

- 証跡PVCがRWXに対応する場合、複数アプリPodから共有できる。
- RWXが利用できない初期環境では、アプリを1Podで稼働させてもよい。
- 将来の複数Pod化を妨げないよう、セッションや業務状態をPodローカルへ保存しない。
- コンテナの一時領域へ永続データを保存しない。

---

## 10. ファイルアップロード

### 10.1 業務上の制限

- アプリ独自のファイルサイズ上限を設けない。
- アプリ独自の1回あたりファイル数上限を設けない。
- 特定の拡張子だけを理由に登録拒否しない。
- 実質的な上限はPVC空き容量、ブラウザ、Route、プロキシ、OpenShift基盤の制約とする。

### 10.2 実装上の必須事項

- ファイル全体をアプリケーションメモリへ読み込まない。
- ストリーミング保存する。
- 大容量ファイルでアプリPodがメモリ不足にならないようにする。
- アップロード進捗を表示する。
- 保存前および保存中にストレージ容量不足を検知する。
- 保存失敗時に不完全ファイルを業務データとして確定しない。
- 一時ファイルを定期削除する。
- ハッシュ値を保存し、破損検出に利用する。
- ファイルをサーバー上で実行しない。
- HTML、実行ファイル等はブラウザ内で実行・表示せず、添付ダウンロードとして扱う。
- SVGは安全な画像へ変換するか、ダウンロード扱いとする。
- ファイル名を安全化し、パストラバーサルを防止する。
- ダウンロード時にもプロジェクト閲覧権限を検証する。

### 10.3 証跡メタデータ

- ID
- project_id
- 関連エンティティ種別
- 関連エンティティID
- 元ファイル名
- 内部保存名
- 保存先キー
- MIMEタイプ
- ファイルサイズ（APIおよびformal JSONでは、`BIGINT UNSIGNED`の精度を失わない10進文字列）
- ハッシュ値
- バージョン番号
- 元ファイルバージョンID
- サムネイル保存先
- 登録者
- 登録日時
- 更新日時
- 論理削除日時
- 論理削除者

---

## 11. バックアップ・復元

### 11.1 保持方針

- 日次バックアップを3世代保持する。
- 週次・月次バックアップは設けない。
- 管理者が手動バックアップを実行できる。
- 利用時間外に実行する。
- 初期値は毎日22:00とする。
- バックアップ先は本番データと同じPVCだけに置かない。

### 11.2 バックアップ単位

1つのバックアップ世代に次を含める。

```text
backup/
└─ <backup-id>/
   ├─ database.sql
   ├─ evidence/
   └─ manifest.json
```

`manifest.json`には次を記録する。

- バックアップID
- 開始日時
- 終了日時
- DBバックアップファイル
- 証跡バックアップ世代
- 対象ファイル数
- 合計サイズ
- 成否
- アプリバージョン
- DBスキーマバージョン

### 11.3 整合性

バックアップは次の順序で実行する。

1. 新規アップロードおよび証跡ファイル更新を一時停止
2. MariaDBの論理バックアップを作成
3. 証跡ファイルをバックアップ
4. manifestを作成
5. 更新を再開
6. 正常終了を記録
7. 4世代目以降を削除

バックアップ失敗時は既存の正常な3世代を削除しない。

### 11.4 手動バックアップ推奨操作

- 大量Excelインポート
- SQLiteデータ移行
- DBスキーマ更新
- プロジェクト一括削除
- バックアップ復元

### 11.5 復元

- 復元は管理者のみ実行可能とする。
- 復元操作は確認画面を表示する。
- 復元前に現在状態を追加バックアップできるようにする。
- DBと証跡は同一backup-idの組み合わせで復元する。
- 復元結果を監査ログへ記録する。

---

## 12. 現行機能

Web版は原則として次の現行機能を引き継ぐ。

### 12.1 テストケース

- フォルダツリー
- 複数フォルダ所属
- タイトル、目的、前提条件、見る場所
- 優先度
- タグ
- 初期データ
- 操作と期待結果の手順
- Markdown編集・プレビュー
- 見る場所画像
- ケース複製
- 複数選択削除
- 検索・フィルター

### 12.2 シナリオ

- タイトル
- 目的
- 前提条件
- ケース追加・除外
- ケース順変更
- フォルダからの作成
- シナリオ削除
- ケース詳細の折りたたみ表示

シナリオ前提条件はWeb版の編集画面へ追加する。

### 12.3 データセット

- `common`
- `case`
- `scenario`
- `run`
- フォルダ共通データ
- ケース・シナリオ・実行への関連付け
- 項目のラベル、値、メモ
- Markdown説明

### 12.4 手順書

- Markdownアップロード
- 見出し構造によるグループ・手順解析
- 再読み込み
- 予定開始・終了・所要時間
- 実施開始・終了・所要時間
- 手順詳細モーダル
- コードブロックのコピー

ローカルファイルの元パスをサーバー側で継続監視する方式は採用しない。再読み込みは新しいファイルの再アップロードとして扱う。

### 12.5 テスト実行

- 実行名
- 環境
- ビルド
- 担当者
- ステータス
- メモ
- シナリオ追加・除外
- ケース状態
- シナリオ状態
- 結果詳細
- 証跡
- 実行日時
- 集計
- 合格率
- 実行率
- スキップ率

---

## 13. 実行スナップショット

### 13.1 作成時点

テスト実行の状態が次のように変更された時点で、実行対象の定義をスナップショット保存する。

```text
draft → in_progress
```

`draft`中は現在のテスト定義を参照する。

開始後にシナリオまたはケースを追加した場合、その追加時点の内容をスナップショット保存する。

### 13.2 保存対象

#### シナリオ

- タイトル
- 目的
- 前提条件
- ケース順
- 元シナリオID
- 元シナリオ更新日時

#### ケース

- タイトル
- 目的
- 前提条件
- 見る場所
- 優先度
- タグ
- 元ケースID
- 元ケース更新日時

#### 手順

- 順番
- 操作
- 期待結果

#### 初期データ

- データセット名
- scope
- 項目ラベル
- 値
- メモ
- 適用理由
  - ケース直接指定
  - フォルダ共通
  - シナリオ指定
  - 実行指定

#### 見る場所画像

- 画像バイナリを実行ごとに複製しない。
- 不変のファイルバージョンIDを参照する。
- 定義画像の編集時は既存ファイルを上書きせず、新しいバージョンを作成する。

### 13.3 DB上の保存形式

スナップショットはJSONだけに保存せず、通常テーブルへ正規化して保存する。

主なテーブル例：

- `run_scenarios`
- `run_scenario_cases`
- `run_case_steps`
- `run_case_data_sets`
- `run_case_data_items`
- `run_case_view_images`

検索・集計・A5:SQL Mk-2での確認を可能にする。

---

## 14. 実行開始後および完了後の変更

### 14.1 変更権限

管理者および割当済み実行者は、`in_progress`の実行を変更できる。`completed`では完了後編集の許可範囲だけを変更できる。

未割当実行者は参照とエクスポートのみ可能とする。

### 14.2 可能な操作

`in_progress`ではシナリオ追加・除外、ケース追加・除外・順序変更、元定義の再取得、結果・実績結果・実行日時・担当者・備考・証跡を変更できる。

`completed`の完了後編集（方針A）では、次の4項目だけを変更できる。

- ケース結果
- 実績結果
- 備考
- 証跡の追加・画像編集・論理削除・復元

シナリオ/ケース構成、順序、元定義の再取得、実行メタデータ、担当者、実行日時は完了後に変更できない。

### 14.3 変更時のルール

- 元定義の変更を自動反映しない。
- 「定義を再取得」操作で明示的に反映する。
- 実行からケースを除外する場合、物理削除せず実行対象外状態として保持する。
- 定義再取得前に変更内容を表示する。
- 既存の結果・証跡に影響する場合は警告を表示する。
- すべての変更前後を監査ログへ記録する。
- スナップショットにリビジョン番号を持たせる。
- 過去リビジョンを参照できるようにする。

### 14.4 完了済み実行

- 読み取り専用にしないが、許可範囲は14.2の方針Aに限定する。
- 再オープン操作は不要とする。
- ステータスは`completed`のまま維持する。
- 許可された変更ではrun version、`post_completion_updated_at`、`post_completion_updated_by`を即時更新し、監査ログに完了後更新として記録する。
- 画面には「完了後更新あり」、最終変更者、最終変更日時を表示する。

---

## 15. 結果・日時・集計

### 15.1 ケース状態

```text
not_run
pass
fail
blocked
skip
```

### 15.2 シナリオ状態

DB・API・UIの正式値はケースと同じ次の6値とする。

```text
not_run
in_progress
pass
fail
blocked
skip
```

シナリオ状態は直接編集せず、対象外でない子ケースだけから毎回自動集計する。優先順は、対象ケース0件は`not_run`、`not_run`または`in_progress`を含む場合は`in_progress`、次に`fail`、`blocked`、`pass`、全件`skip`の順とする。したがって`fail`と`blocked`の混在は`fail`、`pass`と`skip`の混在は`pass`となる。

### 15.3 実績結果

ケース結果画面に`actual_result`入力欄を追加する。

- Markdown対応
- `pass`：任意
- `fail`：必須
- `blocked`：必須
- `skip`：必須

備考と実績結果は別項目として保存する。

### 15.4 日時

- 実行開始日時：`draft`から`in_progress`へ変更した日時
- 実行終了日時：`completed`へ変更した日時
- シナリオ開始日時：最初のケース結果を変更した日時
- シナリオ終了日時：シナリオ結果を確定した日時
- ケース実行日時：手動入力または「今」を設定
- 手動修正可能
- 手動修正は監査対象
- DBはUTC、画面は日本時間

### 15.5 集計

```text
実行率
= (pass + fail + blocked + skip) / 全ケース数

合格率
= pass / (pass + fail + blocked)

スキップ率
= skip / 全ケース数
```

- `skip`は実行済みとして扱う。
- `skip`と`not_run`と`in_progress`は合格率の分母に含めない。
- 分母が0の場合、API値は`null`、画面表示は「算出対象なし」とする。
- スキップ理由は必須とする。

---

## 16. 削除・復元

### 16.1 削除権限

管理者および割当済み実行者は、担当プロジェクト内の次のデータを削除できる。

- テストケース
- シナリオ
- データセット
- 手順書
- テスト実行
- 証跡

管理者のみが次を実行できる。

- プロジェクトアーカイブ
- プロジェクト物理削除
- ユーザー無効化
- バックアップ復元

### 16.2 論理削除

通常の削除は論理削除とする。

共通項目：

- `deleted_at`
- `deleted_by`
- `delete_reason`

削除データは通常一覧から除外する。

### 16.3 復元期間

| 対象 | 復元可能期間 |
|---|---:|
| 通常業務データ | 30日 |
| 証跡ファイル | 30日 |
| プロジェクト | 90日 |
| ユーザー | 物理削除せず無効化 |

復元期限を過ぎたデータは定期処理で物理削除する。

### 16.4 過去実行

- 元ケースまたは元シナリオが削除されても、実行スナップショットは削除しない。
- 現在のシナリオでは、削除済みケースであることを表示する。
- 過去実行では、スナップショットに保存された内容を表示する。
- 実行スナップショットへの外部キーは、元定義削除で消えない設計にする。

---

## 17. 監査ログ

### 17.1 記録対象

- ログイン成功
- ログイン失敗
- ログアウト
- ユーザー作成・変更・無効化
- パスワード再設定
- ロック解除
- プロジェクト作成・変更・アーカイブ
- プロジェクト割当変更
- ケース作成・変更・削除・復元
- シナリオ作成・変更・削除・復元
- データセット作成・変更・削除・復元
- 手順書取込・更新・削除・復元
- 実行作成・開始・完了
- 完了後の実行変更
- 結果変更
- 日時変更
- スナップショット再取得
- ケース・シナリオ追加、除外、順序変更
- 証跡追加・編集・削除・復元
- インポート
- エクスポート
- バックアップ
- バックアップ復元
- 物理削除

### 17.2 記録項目

- ID
- 発生日時
- ユーザーID
- ユーザー名
- project_id
- action
- entity_type
- entity_id
- 変更前
- 変更後
- request_id
- クライアントIP
- User-Agent
- 成否
- エラーコード

### 17.3 保持と変更

- 監査ログは通常ユーザーから変更・削除できない。
- 監査ログの保持期間は2年とする。
- 監査ログの閲覧範囲は権限に応じて制御する。

---

## 18. 同時編集

### 18.1 楽観ロック

- 更新対象レコードに`version`を持たせる。
- 更新APIはクライアントが取得したversionを送信する。
- DB更新時にversion一致を検証する。
- 一致しない場合はHTTP 409相当の競合エラーとする。
- 競合時に無条件上書きしない。
- 最新内容と利用者の入力内容を比較表示する。
- 初期版ではMarkdownの自動マージを行わない。

### 18.2 自動保存

- 実行結果、担当者、実行日時：自動保存
- ケース・シナリオ定義：明示的保存
- 保存状態を画面に表示する。
- 保存失敗を成功として表示しない。
- 通信切断中の入力を誤って確定扱いにしない。
- 長文Markdownはブラウザ側に一時下書きを保持してよい。

---

## 19. Markdown

### 19.1 許可する表現

- 見出し
- 段落
- 改行
- 箇条書き
- 番号付きリスト
- 入れ子リスト
- タスクリスト
- 引用
- 水平線
- コードブロック
- インラインコード
- 表
- 太字
- 斜体
- 打ち消し
- `http`および`https`リンク

### 19.2 禁止する表現

- 生HTML
- `script`
- `iframe`
- JavaScript URL
- 危険なURLスキーム
- 任意のstyle属性
- 外部画像の直接読み込み
- Data URLによる任意コンテンツ
- スクリプトを含むSVG

Markdownは表示前に必ずサニタイズする。

画像は原則としてアプリへアップロードしたファイルを利用する。

---

## 20. インポート・エクスポート

### 20.1 正式形式

- 完全な移行・バックアップ用の正式形式は、`schema_version`付きJSONとする。
- CSVとMarkdownは表示・簡易交換用途とする。
- JSONでは操作と期待結果の対応、シナリオ順、初期データ、実行スナップショットを保持する。
- 証跡バイナリはmanifestと分離して管理する。

### 20.2 エクスポート権限

ログイン済みユーザーは、プロジェクト割当の有無に関係なく、閲覧可能なすべてのプロジェクトをエクスポートできる。

エクスポート操作は監査ログへ記録する。

### 20.3 Excelインポート

正式対応形式は`.xlsx`とする。

初期対応シート：

- `Cases`
- `Steps`

次段階で追加するシート：

- `Scenarios`
- `ScenarioCases`
- `DataSets`
- `DataItems`

### 20.4 `Cases`シート

| 列 | 内容 |
|---|---|
| CaseKey | Excel内でケースを識別するキー |
| FolderPath | 例：`機能/ログイン` |
| Title | ケースタイトル |
| Objective | 目的 |
| Preconditions | 前提条件 |
| ViewLocation | 見る場所 |
| Priority | 高・中・低 |
| Tags | カンマ区切り |

### 20.5 `Steps`シート

| 列 | 内容 |
|---|---|
| CaseKey | Casesとの関連キー |
| StepNo | 手順順 |
| Action | 操作 |
| Expected | 期待結果 |

### 20.6 インポート処理

1. 公式テンプレートをダウンロード
2. `.xlsx`をアップロード
3. サーバーで解析
4. エラー・警告をプレビュー
5. 新規・更新・スキップ件数を表示
6. 利用者が確定
7. 1トランザクションで登録
8. 結果レポートを出力

### 20.7 検証

- 必須列
- タイトル空欄
- CaseKey重複
- StepNo重複
- 存在しないCaseKey
- 不正な優先度
- 存在しないフォルダ
- セル文字数超過
- 数式セル
- 結合セル
- 非表示行
- 改行
- Markdown
- パスワード付きファイル
- 外部参照

### 20.8 初期非対応

- `.xls`
- `.xlsm`
- マクロ
- パスワード付きExcel
- 埋め込み画像
- ローカルファイルパスによる証跡指定

### 20.9 重複時の扱い

- アプリからエクスポートしたExcelは内部ID一致で更新可能とする。
- 新規テンプレートのCaseKeyはExcel内だけの識別子とする。
- タイトル一致だけでは自動更新しない。
- 重複候補はインポートプレビューで確認する。

---

## 21. Web一括編集

テスト作成を容易にするため、表形式の一括編集画面を追加する。

- Excelから複数セルを貼り付け
- 複数行追加
- 行削除
- 行複製
- タグ一括設定
- フォルダ一括設定
- キーボード移動
- セル単位のエラー表示
- 未保存行の強調
- 複数ケースの一括保存
- 大量データのページングまたは仮想スクロール

一括編集でも通常の権限、監査、楽観ロックを適用する。

---

## 22. 画像プレビュー・編集

現行機能を引き継ぐ。

- 画像プレビュー
- トリミング
- ペイント
- 枠線
- undo / redo
- 元画像への復元
- 並び順変更
- クリップボードからの画像貼り付け
- サムネイル生成

編集時は既存画像を上書きせず、ファイルバージョンを追加する。
過去実行が参照している画像バージョンを変更しない。

大容量画像はサムネイルを表示し、原本は認可付きダウンロードとする。

---

## 23. API方針

主なAPIグループ：

```text
/api/auth
/api/users
/api/projects
/api/project-assignments
/api/test-cases
/api/folders
/api/scenarios
/api/data-sets
/api/procedures
/api/runs
/api/evidence
/api/imports
/api/exports
/api/audit-logs
/api/backups
/api/system
```

### 23.1 共通要件

- JSON API
- OpenAPI定義
- 入力スキーマ検証
- 認証必須
- project_id検証
- 権限検証
- request ID
- 構造化ログ
- 統一エラーレスポンス
- ページング
- 楽観ロック
- トランザクション
- 監査ログ連携

---

## 24. バックグラウンド処理

次の処理は同期HTTPリクエストから分離できる構成にする。

- 大量Excelインポート
- 大量エクスポート
- サムネイル生成
- 画像一括変換
- SQLite移行
- 復元期限超過データの物理削除
- バックアップ
- バックアップ復元

初期版ではRedisを必須としない。
MariaDB上のジョブテーブルまたはOpenShift Job / CronJobを利用してよい。

ジョブには次を持たせる。

- 状態
- 進捗
- 開始日時
- 終了日時
- 実行者
- project_id
- 成功件数
- 失敗件数
- エラー詳細
- 結果ファイル

---

## 25. OpenShift要件

- コンテナは非rootで動作する。
- 任意UIDで起動可能とする。
- `0.0.0.0`で待ち受ける。
- `PORT`環境変数へ対応する。
- `/healthz`を実装する。
- `/readyz`を実装する。
- MariaDB接続失敗をreadinessへ反映する。
- 必要なストレージ接続失敗をreadinessへ反映する。
- DBマイグレーション失敗を起動ログへ明示する。
- RouteはTLSを使用する。
- SecretとConfigMapを分離する。
- 機密情報をイメージへ埋め込まない。
- コンテナ内の一時領域へ永続業務データを保存しない。
- 構造化ログを標準出力へ出す。
- Resource requests / limitsを定義する。
- ServiceAccountを定義する。
- 必要に応じてNetworkPolicyを定義する。
- CronJobで日次バックアップを実行する。
- 初期構成は1Podでもよいが、将来の複数Pod化を妨げない。

---

## 26. ログ・監視

### 26.1 構造化ログ

最低限、次を含める。

- timestamp
- level
- message
- request_id
- user_id
- project_id
- route
- HTTP method
- status
- elapsed_ms
- error_code

パスワード、セッションID、機密な初期データ内容を不用意に出力しない。

### 26.2 監視

- HTTP応答
- MariaDB接続
- PVC使用量
- 一時領域使用量
- バックアップ成否
- ジョブ失敗
- アップロード失敗
- 5xx件数
- ログイン失敗件数

PVC使用量が閾値を超えた場合、管理者画面へ警告を表示する。

---

## 27. データ移行

### 27.1 方針

既存SQLiteプロジェクトをMariaDBのプロジェクトへ移行するワンショットツールを作成する。

### 27.2 移行単位

- 1つのSQLiteプロジェクトフォルダを、1つのWeb版プロジェクトへ移行する。
- `the-test.sqlite`と`attachments/`をセットで入力する。
- 移行先プロジェクトを新規作成または指定する。
- ID対応表を保存する。
- 既存データを直接上書きしない。
- 移行前に検証する。
- 移行結果レポートを生成する。

### 27.3 正式移行形式

バージョン付きJSONと証跡manifestを中間形式として利用してよい。

```json
{
  "schema_version": "1.0",
  "project": {},
  "folders": [],
  "test_cases": [],
  "scenarios": [],
  "data_sets": [],
  "test_runs": [],
  "evidence_manifest": []
}
```

### 27.4 移行時の注意

- 現行CSV、JSON、Markdownは完全な往復互換を保証しない。
- 操作と期待結果の関係を失わない。
- シナリオケース順を保持する。
- 証跡はDBメタデータと実ファイルの両方を確認する。
- 現行の過去実行は不足しているスナップショット項目を可能な範囲で補完する。
- 補完できない項目は移行レポートへ記録する。

---

## 28. 実装フェーズ

### Phase 0：仕様・現行資産固定

1. 本書を基準仕様とする。
2. 現行SQLiteと証跡をバックアップする。
3. 代表的なCSV、JSON、Markdownを保存する。
4. 画面別の受け入れ試験を作成する。
5. MariaDBスキーマを確定する。
6. API一覧を確定する。

### Phase 1：基盤

1. Webプロジェクトを新規構築
2. MariaDB
3. DBマイグレーション
4. ローカル認証
5. ユーザー管理
6. プロジェクト管理
7. 権限制御
8. 監査ログ
9. `/healthz`
10. `/readyz`
11. OpenShiftマニフェスト

### Phase 2：定義系

1. テストケース
2. フォルダ
3. シナリオ
4. データセット
5. Markdown
6. 一括編集
7. Excelインポート初期版

### Phase 3：実行系

1. テスト実行
2. スナップショット
3. リビジョン
4. 実績結果
5. 実行日時
6. 合格率・実行率・スキップ率
7. 完了後編集
8. 同時編集制御

### Phase 4：証跡・手順書

1. PVCストレージ
2. アップロード
3. プレビュー
4. サムネイル
5. 画像編集
6. 手順書取込
7. 予定・実績管理

### Phase 5：入出力・移行・運用

1. 正式JSON
2. CSV・Markdown
3. Excel追加シート
4. SQLite移行
5. バックアップ
6. 復元
7. 物理削除ジョブ
8. 負荷試験
9. 復元試験
10. 権限試験

---

## 29. 主要受け入れ条件

### 29.1 認証

- 管理者が自由なユーザー名でユーザーを作成できる。
- 4文字のパスワードでログインできる。
- パスワードが平文保存されない。
- 初回ログイン時に仮パスワード変更が要求される。
- 5回連続失敗で5分ロックされる。
- 無効ユーザーはログインできない。

### 29.2 権限

- 管理者は全プロジェクトを更新できる。
- 割当済み実行者は担当プロジェクトを更新・削除・復元できる。
- 未割当実行者は更新できない。
- 未割当実行者も閲覧・エクスポートできる。
- APIへ直接不正なproject_idを送っても更新できない。

### 29.3 スナップショット

- `draft`から`in_progress`への変更時に定義が固定される。
- 元ケース変更後も、実行側の内容が自動で変わらない。
- 定義再取得時にリビジョンが増える。
- 過去リビジョンを参照できる。
- 元ケース削除後も実行内容を表示できる。

### 29.4 完了後編集

- 完了済み実行を管理者と割当済み実行者が変更できる。
- 完了後変更が画面で識別できる。
- 変更前後が監査ログへ残る。

### 29.5 削除・復元

- 通常削除でデータが即時物理削除されない。
- 30日以内の通常データを復元できる。
- 90日以内のプロジェクトを復元できる。
- 元ケース削除で過去実行が消えない。

### 29.6 Excel

- 公式`.xlsx`テンプレートを出力できる。
- CasesとStepsを取り込める。
- インポート前にエラーを確認できる。
- エラー時に一部だけ登録されない。
- タイトル一致だけで既存ケースを自動更新しない。

### 29.7 バックアップ

- 日次バックアップが作成される。
- 正常な3世代が保持される。
- 4世代目作成後に最古世代が削除される。
- 失敗時に既存正常世代が保持される。
- DBと証跡を同じbackup-idで復元できる。

### 29.8 OpenShift

- 非root・任意UIDで起動できる。
- `PORT`環境変数で待受ポートを変更できる。
- `/healthz`と`/readyz`が動作する。
- MariaDB停止時にreadyではなくなる。
- Pod再起動後もDBと証跡が保持される。

---

## 30. Codexへの実装指示

1. 本書を最優先仕様として扱うこと。
2. 旧仕様と矛盾する場合は、本書を優先すること。
3. Electron固有API、preload、IPCをWeb版へ持ち込まないこと。
4. MariaDB、PVC、ローカル認証を前提にすること。
5. 画面だけでなくAPIでも必ず権限検証すること。
6. 実行スナップショットを元定義から独立させること。
7. 完了済み実行を編集可能にし、変更履歴を失わないこと。
8. 削除は原則論理削除とし、過去実行へ影響させないこと。
9. エクスポートは全ログインユーザーに許可すること。
10. ファイルサイズのアプリ独自上限を設けないこと。
11. ファイルをメモリへ全量展開しないこと。
12. SQLスキーマ変更をマイグレーションで管理すること。
13. A5:SQL Mk-2で主要データを確認できる正規化構造にすること。
14. JSON列だけに業務スナップショットを閉じ込めないこと。
15. 実装と同時に自動テストを作成すること。
16. OpenAPI、DBスキーマ、マイグレーション、受け入れ条件をコードと同期させること。
17. 未確定として推測で仕様を追加せず、必要な場合は明示的な実装上の仮定として記録すること。

---

## 31. 確定事項まとめ

- 複数プロジェクト対応
- 1ユーザー複数プロジェクト参加
- ロールは管理者・実行者の2種類
- 未割当プロジェクトは閲覧・エクスポート可能
- OpenShift OAuthは不使用
- ユーザー名・パスワード認証
- ユーザー名は自由入力
- パスワードは4文字以上、種類強制なし、脆弱パスワード許可
- MariaDB採用
- A5:SQL Mk-2で管理可能な構造
- DB 20GiB
- 証跡PVC 100GiB
- 一時領域10GiB
- アップロードのアプリ独自上限なし
- 日次バックアップ3世代
- `draft`から`in_progress`でスナップショット作成
- スナップショットは正規化テーブルへ保存
- 見る場所画像は不変バージョン参照
- 開始後・完了後も変更可能
- 変更履歴とリビジョンを保持
- 実行者も担当プロジェクト内の削除・復元が可能
- 論理削除
- 通常データ30日、プロジェクト90日復元可能
- Excelテンプレート取込
- Web表形式一括編集
- シナリオ前提条件追加
- 実績結果追加
- `skip`は合格率から除外
- Markdownをサニタイズ
- 楽観ロック
- Web基盤は新規構築し、既存資産を選択移植
