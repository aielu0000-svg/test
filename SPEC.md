# ザ・テスト 現行仕様書・Web化移行仕様

## 1. 文書情報

| 項目 | 内容 |
| --- | --- |
| 文書名 | ザ・テスト 現行仕様書・Web化移行仕様 |
| 対象バージョン | 0.1.0 |
| 調査日 | 2026-07-27 |
| 調査対象 | このワークスペースのローカル実装、設計メモ、サンプルデータ、GitHub Actions設定 |
| 製品の位置付け | テストケース、シナリオ、初期データ、手順書、テスト実行結果、証跡を管理するアプリ |
| 現行実行形態 | Electronデスクトップアプリ、ローカルSQLite、単一端末利用 |
| 最終目標 | OpenShift上で稼働するWebアプリケーション |

### 1.1 この文書の読み方

- 「現行仕様」は、現在のソースコードが実際に行っている動作を記載する。
- 「現行制約・注意」は、現行実装で仕様上の注意が必要な点、または画面説明とコードの差異を記載する。
- 「Web化方針」は、OpenShift対応に向けた移行時の設計方針であり、まだ実装済みではない。
- 今後の改修では、現行データを引き継ぐ必要がある機能と、Web化に伴って仕様を変更できる機能を分けて判断する。

## 2. 現状の概要

### 2.1 アプリの目的

テスト作業に必要な以下の情報を一つのプロジェクト単位で管理する。

1. テストケースの定義
2. テストケースをまとめたシナリオの定義
3. テスト前提となる初期データの定義
4. Markdown手順書の取り込みと予定・実施時間の管理
5. シナリオ単位のテスト実行
6. テストケース単位の合否・ブロック・スキップ・実績の記録
7. シナリオおよびテストケースの証跡ファイル管理
8. CSV、JSON、Markdownによるテスト資産の入出力
9. プロジェクトDBと添付ファイルのバックアップ

### 2.2 現行アプリの特徴

- アプリ単体では外部サーバーへ通信しないローカル完結型である。
- ElectronのメインプロセスがSQLiteとファイルシステムを操作する。
- Reactのレンダラ画面からは、preloadで公開されたAPIだけを呼び出す。
- プロジェクトは任意のフォルダに作成され、その中にDBと添付ファイルを保存する。
- ケース、シナリオ、実行結果などは、削除や保存を画面操作で行える。
- ダークテーマが初期値で、ライトテーマにも切り替えられる。

## 3. 技術構成・実行構成

### 3.1 技術スタック

| 層 | 現行技術 | 主な役割 |
| --- | --- | --- |
| デスクトップシェル | Electron 29系 | ウィンドウ、ファイルダイアログ、クリップボード、OSファイルオープン |
| UI | React 18、TypeScript | 画面表示、状態管理、入力、プレビュー |
| ビルド | Vite、electron-vite | main/preload/rendererのビルド |
| CSS | Tailwind CSS、PostCSS | テーマ、レスポンシブレイアウト |
| DB | better-sqlite3 | プロジェクトごとのSQLite保存 |
| 配布 | electron-builder、NSIS | Windowsインストーラー（`.exe`） |
| CI | GitHub Actions | `main`へのpushまたは手動実行でWindows版をビルドし、artifactに保存 |

`package.json`の主なコマンドは以下のとおり。

| コマンド | 内容 |
| --- | --- |
| `npm run dev` | Electron開発起動 |
| `npm run build` | Electronアプリを`out/`へビルド |
| `npm run package` | ビルド後にelectron-builderでパッケージ化 |
| `npm run package:win` | Windows x64のNSISインストーラーを作成 |

### 3.2 Electronのプロセス分離

- main process: `src/main/index.ts`
- DB・ファイル処理: `src/main/db.ts`
- Markdown手順書パーサー: `src/main/procedureMarkdown.ts`
- preload bridge: `src/preload/index.ts`
- renderer entry: `src/renderer/src/App.tsx`
- `contextIsolation: true`
- `nodeIntegration: false`
- rendererからNode.jsやSQLiteを直接操作せず、preloadのAPIを経由する。

### 3.3 プロジェクトフォルダ

プロジェクト作成時に指定したフォルダへ、原則として以下を作成する。

```text
<project-folder>/
├─ the-test.sqlite
└─ attachments/
   ├─ <uuid>_<safe-file-name>
   └─ <uuid>_original_<safe-file-name>  # 初回編集前の元画像バックアップ
```

- `the-test.sqlite`に定義・結果・ファイルメタデータを保存する。
- `attachments/`に証跡の実体を保存する。
- DB単体では完全なプロジェクトにならず、添付フォルダも必要である。

## 4. 共通UI仕様

### 4.1 プロジェクト未選択時

起動時にプロジェクトが開かれていない場合、中央カードを表示する。

| 項目 | 仕様 |
| --- | --- |
| アプリ名 | ザ・テスト |
| 説明 | ローカルで完結するテスト管理アプリ。データベースと証跡フォルダごと持ち運べる旨を表示 |
| 入力 | プロジェクト名。初期値は「ザ・テスト」 |
| 操作 | 「新規プロジェクトを作成」「既存プロジェクトを開く」 |
| エラー | キャンセル、保存先不正、DB不在などをカード内に表示 |

#### 新規プロジェクト

1. プロジェクト名を入力する。
2. 保存先フォルダ選択ダイアログを開く。
3. 指定フォルダにSQLite DBと`attachments`フォルダを作成する。
4. DBのメタ情報に名前、作成日時、更新日時を保存する。
5. ダッシュボードを表示する。

#### 既存プロジェクト

- `.sqlite`ファイルを選択する。
- DBと同じフォルダをプロジェクトフォルダとして扱う。
- 指定DBがない場合はエラーにする。

### 4.2 共通レイアウト

#### デスクトップ

- 左サイドバーは初期表示する。幅は約280px。
- アプリ名ボタンを押すとダッシュボードへ移動する。
- サイドバーはヘッダーの「サイドメニューを隠す／表示」で切り替えられる。
- サイドバー下部にプロジェクト名、プロジェクトパス、設定ボタンを表示する。

#### メニュー

| グループ | メニュー | 画面キー |
| --- | --- | --- |
| 概要 | ダッシュボード | `dashboard` |
| 概要 | テストケース | `cases` |
| 概要 | シナリオ | `scenarios` |
| 実行 | 手順書 | `procedures` |
| 実行 | テスト実行 | `runs` |
| 実行 | データセット | `data` |
| レポート | エクスポート / インポート | `export` |
| システム | 設定 | `settings` |

#### ヘッダー

- 現在画面のタイトルとサブタイトルを表示する。
- デスクトップではサイドバー表示切替を表示する。
- モバイル幅では「メニュー」ボタンを表示し、折りたたみナビゲーションを表示する。
- ダッシュボードでは「新規テスト実行」ボタンを表示する。
- テーマ選択（ダーク／ライト）を表示する。

#### テーマ・表示設定

- 初期テーマはダーク。
- ライト／ダークは`localStorage`に保存する。
- 文字サイズは14～36px、初期値21px。`localStorage`に保存する。
- 証跡画像の自動サイズ調整は初期有効。
- 画像の最大横幅・最大縦幅は400～4000px、初期値1600px。`localStorage`に保存する。

### 4.3 Markdown入力・プレビュー

目的、前提、見る場所、備考、手順、期待結果などの長文欄は、編集／プレビューを切り替えられるMarkdownエディターを使用する。

主な表示対応は以下のとおり。

- 見出し（`#`～`######`）
- 段落、改行
- 箇条書き、番号付きリスト、インデントされた入れ子リスト
- タスクリスト（`[ ]`、`[x]`）
- 引用（`>`）
- 水平線
- コードブロック、言語表示
- 表
- インラインコード
- 太字、斜体、打ち消し
- リンク、画像、`<br>`

Web化時には、リンク・画像・HTMLの扱いをサニタイズし、外部URLやスクリプトがそのまま実行されないようにする。

## 5. 画面一覧

| ID | 画面・モーダル | 主目的 |
| --- | --- | --- |
| SCR-01 | プロジェクト開始 | 新規作成または既存プロジェクトを開く |
| SCR-02 | ダッシュボード | 件数、進捗、合格率、最近の実行を俯瞰する |
| SCR-03 | テストケース一覧 | ケース検索、フォルダ整理、複数選択、削除、詳細への遷移 |
| SCR-04 | テストケース詳細 | ケースの定義、手順、初期データ、見る場所画像を編集する |
| SCR-05 | シナリオ一覧 | シナリオ検索、作成、フォルダからの自動生成、削除 |
| SCR-06 | シナリオ詳細 | 目的とケース構成、ケース順を編集する |
| SCR-07 | 手順書 | Markdownを取り込み、予定・実績を管理する |
| SCR-08 | データセット | 初期データの定義、スコープ、紐付けを管理する |
| SCR-09 | テスト実行一覧 | 実行を検索、状態別に一覧、作成・削除する |
| SCR-10 | テスト実行詳細 | 実行メタ情報と対象シナリオを管理する |
| SCR-11 | シナリオ実行 | ケースごとの結果、実績、備考、証跡を記録する |
| SCR-12 | エクスポート／インポート | テスト資産のファイル入出力を行う |
| SCR-13 | 設定 | プロジェクト名、外観、画像設定、DB操作を管理する |
| MOD-01 | 手順詳細 | 手順本文・コード・リストを読む、開始・終了を打刻する |
| MOD-02 | 証跡プレビュー／画像編集 | 画像確認、トリミング、ペイント、枠線、復元、保存 |
| MOD-03 | フォルダダイアログ | フォルダ作成・名前変更 |
| MOD-04 | 確認ダイアログ | データ削除、DBリセットの確認 |

## 6. 画面詳細仕様

### SCR-02 ダッシュボード

#### 表示項目

4つのメトリクスカードを表示する。

| カード | 集計内容 | 操作 |
| --- | --- | --- |
| テストケース総数 | DB内のテストケース件数 | 「開く」でテストケース画面へ |
| シナリオ | DB内のシナリオ件数 | 「開く」でシナリオ画面へ |
| 実行中のテスト実行 | `completed`以外の実行件数 | 「開く」でテスト実行画面へ |
| 合格率 | ケース結果の`pass` ÷ `pass/fail/blocked`件数 | 「表示」でテスト実行画面へ |

補足表示。

- テストケースカードには直近7日間の新規件数を表示する。
- シナリオカードには直近30日間に更新された件数を「アクティブ」として表示する。
- 実行カードには`in_progress`の件数も表示する。
- 合格率には、算出できる場合に前週との差を表示する。
- `skip`は合格率の分母に含めない。

#### 最近のテスト実行

- 更新日時順で最大3件を表示する。
- 列は実行名、状態、進捗、日付。
- 行クリックで該当実行の詳細へ移動する。
- 実行がない場合は空状態メッセージを表示する。
- 「新規テスト実行」から新規実行作成画面を開く。

### SCR-03 テストケース一覧

#### フォルダペイン

- 「すべてのテストケース」「未分類」を表示する。
- case folderを親子ツリーで表示する。
- 各フォルダに所属ケース件数を表示する。
- 子フォルダを展開／折りたたみできる。
- フォルダ選択時は子孫フォルダ内のケースも一覧対象にする。
- 「フォルダ追加」でルートまたは選択中フォルダ直下に作成する。
- フォルダの右クリックメニューで名前変更、コピー、貼り付け、削除を行う。
- フォルダをドラッグして別フォルダ配下またはルートへ移動できる。
- 自分自身または子孫へ移動する操作は拒否する。

#### ケース一覧ペイン

- タイトル検索。現行実装ではタイトルだけを対象に部分一致検索する。
- 優先度フィルター（すべて／高／中／低）。
- タグフィルター。複数タグ選択時は、いずれかに一致するケースを表示する。
- 一覧行にはタイトル、目的、優先度、先頭から最大3個のタグを表示する。
- 単クリックで選択する。
- Ctrl／Commandクリックで複数選択する。
- Shiftクリックで範囲選択する。
- ダブルクリックで詳細画面を開く。
- 「複製」で既存ケースを元に新規編集状態を作成する。
- 「削除」で個別削除確認を行う。
- 画面右下の「選択削除」で、現在の絞り込み対象の選択ケースをまとめて削除する。
- 「新規テストケース」または「このフォルダで新規作成」で詳細画面を開く。

#### ケース一覧の削除

- テストケース削除時は関連する手順、フォルダ所属、ケース関連データリンクを削除する。
- DBの外部キーにより、実行結果側の関連データも削除される可能性があるため、Web化時は削除影響を明示する。

### SCR-04 テストケース詳細

#### 入力項目

| 項目 | 必須 | 内容 |
| --- | --- | --- |
| タイトル | 必須 | ケース名。空欄では保存不可 |
| 目的 | 任意 | Markdown本文 |
| 前提条件 | 任意 | Markdown本文 |
| 見る場所 | 任意 | 確認対象の画面、項目、位置など。Markdown本文 |
| 優先度 | 任意 | 高／中／低。初期値は中 |
| タグ | 任意 | カンマ区切り文字列。例：`ログイン,UI` |
| フォルダ | 任意 | 複数フォルダをチェック選択可能 |
| 初期データ | 任意 | 個別データセットを関連付ける。フォルダ共通データは自動適用 |
| 手順 | 任意 | 1件以上の操作・期待結果の組。空欄組は保存時に除外 |

#### 手順編集

- 「手順を追加」で操作／期待結果の組を追加する。
- 各手順を削除できる。ただし画面上は最低1行を残す。
- 操作と期待結果はMarkdownで入力・プレビューできる。
- 保存時はposition順で`test_steps`に保存する。

#### 見る場所の画像

- 既存ケースを保存した後に利用できる。
- 画像ファイル追加、クリップボード貼り付け、プレビュー／編集、削除、上下移動ができる。
- 対象はPNG、JPG、JPEG、GIF、WebP、BMP、SVG。
- 並び順はDBに保存する。

#### 保存・複製

- 保存ボタンは画面右下に固定表示する。
- タイトルが空欄の場合、「タイトルは必須です。」を表示する。
- 新規保存後は一覧へ戻る。
- 編集中ケースから「複製して新規」を選択できる。
- 画面上のケース複製では、定義、手順、タグ、フォルダ、データセット関連を引き継ぐ。
- 現行画面の複製導線では、見る場所の画像は引き継がない。

### SCR-05 シナリオ一覧

- シナリオタイトルの部分一致検索を行う。
- カードにタイトル、目的、テストケース件数、画面上の状態ラベルを表示する。
- 状態ラベルは実行結果ではなく、現行UI上の簡易状態である。
  - ケース0件：計画中
  - 作成日時と更新日時が同じ：レビュー中
  - その他：進行中
- 「新規シナリオ」で詳細画面を開く。
- フォルダと任意のシナリオ名を指定して「フォルダから作成」できる。
- フォルダから作成時は、そのフォルダに所属するケースを作成日時順で追加する。
- シナリオ削除は確認ダイアログを表示する。

### SCR-06 シナリオ詳細

#### 入力項目

| 項目 | 必須 | 内容 |
| --- | --- | --- |
| タイトル | 必須 | シナリオ名 |
| 目的 | 任意 | Markdown本文 |
| テストケース | 任意 | 既存ケースを追加、除外、並べ替え |

#### ケース構成

- 追加候補は、現在のシナリオに未追加のケースだけを表示する。
- セレクトでケースを選び、「追加」で末尾に追加する。
- 各ケースはドラッグ＆ドロップで順番を変更する。
- 「除外」でシナリオから外す。ケース定義そのものは削除しない。
- 保存時にシナリオと`scenario_cases.position`を一括更新する。
- 既存シナリオでは、ケースごとの前提、見る場所、初期データ、手順、期待結果を折りたたみ表示する。
- ケース詳細内の「シナリオから除外」でも除外できる。

#### 現行制約

- DBにはシナリオ前提条件欄があるが、現行のシナリオ詳細画面には編集欄がない。
- Markdownサンプルにはシナリオ前提の記法があるが、画面保存では入力できない。
- フォルダからの自動生成は、選択フォルダに直接所属するケースを対象とし、子孫フォルダを再帰的には集めない。

### SCR-07 手順書

#### 取り込み

- 「Markdown取込」で`.md`または`.markdown`ファイルを選択する。
- ファイル名を元にタイトルを仮決定し、Markdownの最初の`#`見出しがあればタイトルにする。
- `#`を第1グループ、`##`を第2グループ、`###`を一覧上の手順として解釈する。
- `####`以下は手順本文内の見出しブロックとして扱う。
- 取り込み時に、段落、コード、見出し、リスト、水平線をブロックとして保持する。
- 既に同じsource pathの文書がある場合は同一文書を更新する。
- 手順はstable key、path、見出し、本文ハッシュで既存手順との対応を試み、既存の実施状態を維持する。

#### 文書一覧

- 取り込み済み文書をDocumentsペインに表示する。
- 文書名、元ファイル名、手順数、更新日を表示する。
- 文書選択で右側に詳細を表示する。
- 「再読み込み」で元ファイルを再解析する。

#### 予定日時モード

一覧列は、No、手順、予定開始、予定終了、予定所要、状態。

- 開始・終了は`datetime-local`で入力する。
- 所要は`HH:mm`形式で入力する。例：`00:10`、`01:30`。
- 開始と終了を入力すると所要を分単位で算出する。
- 所要と開始を入力すると終了を算出する。
- 先頭から所要時間を積み上げて後続予定を再計算する。
- 途中の手順を手動起点にすると、その手順以降を変更後の日時から再計算する。
- 手動起点より前の予定は維持する。

#### 実施日時モード

- 開始・終了を入力する。
- 所要は開始と終了の差分から分単位で算出する。
- 終了ありは完了、開始のみは実施中、どちらもなしは未実施とする。
- 手順詳細モーダルの「今開始」「今終了」で現在時刻を記録できる。

#### 手順詳細モーダル（MOD-01）

- 手順名、所属グループ、本文ブロックを表示する。
- コードブロックは言語名、横スクロール、Copyボタンを表示する。
- 長文はモーダル内スクロールする。
- 閉じても一覧の入力値を保持する。

### SCR-08 データセット

#### 一覧

- データセット名の部分一致検索を行う。
- 種別で絞り込む。
- 一覧には名称、種別、共通データの場合はフォルダ名を表示する。
- 「新規作成」で空の詳細を表示する。

#### 種別（scope）

| 値 | 表示 | 適用対象 |
| --- | --- | --- |
| `common` | 共通初期データ | 全体または指定フォルダのケース |
| `case` | テストケース初期データ | チェックしたケース |
| `scenario` | シナリオ初期データ | チェックしたシナリオ |
| `run` | 実行初期データ | チェックしたテスト実行 |

#### 詳細

- 名称は必須。
- 種別を選択する。
- `common`の場合、全体またはフォルダを指定できる。
- `common`以外は対象ケース、シナリオ、実行を複数チェックして紐付ける。
- 説明はMarkdownで入力する。
- 項目はラベル、値、メモを持つ。
- 値とメモは改行可能。
- 項目を追加、削除できる。
- 空のラベルかつ空の値の行は保存時に除外する。
- 保存・削除を行う。
- テンプレート作成でscopeごとのひな形を作成できる。

#### ケースへの適用

- ケースに直接関連付けたデータセットを表示する。
- ケース所属フォルダに設定された`common`データセットを自動適用する。
- 自動適用されたフォルダ共通データはケース画面で選択解除できない。
- シナリオ詳細と実行詳細では、ケースに実際に適用されるデータ内容を展開表示する。

### SCR-09 テスト実行一覧

- 実行名の部分一致検索を行う。
- タブで以下を切り替える。
  - 進行中：`completed`以外
  - 過去の実行：`completed`
  - draft：`draft`
- 列は実行名、シナリオ件数、状態、進捗、操作。
- 進捗は、実行に含まれるケースのうち`not_run`以外の割合。
- 「開く」で実行詳細へ移動する。
- 「新しい実行を開始」で新規詳細へ移動する。
- 削除時は確認ダイアログを表示する。

### SCR-10 テスト実行詳細

#### 実行メタ情報

| 項目 | 必須 | 内容 |
| --- | --- | --- |
| 実行名 | 必須 | 実行単位の名前 |
| 環境 | 任意 | 環境名、URLなど |
| ビルド | 任意 | ビルド番号、バージョン |
| 担当者 | 任意 | 実行担当者。入力時、担当未設定のシナリオへ初期反映 |
| ステータス | 任意 | draft／実行中／完了 |
| メモ | 任意 | Markdown本文 |

- 保存ボタンは固定表示する。
- 既存実行のメタ情報は変更後約600msで自動保存する。
- 新規実行は、最初の保存またはシナリオ追加時に保存される。
- DBには開始日時・終了日時欄があるが、現行画面には編集入力がない。インポートや内部データに値がある場合だけ保持する。

#### シナリオ追加

- シナリオ名で検索し、追加候補から選択して追加する。
- 同じシナリオを同一実行に二重追加しない。
- 追加時点のシナリオケース順を`run_scenario_cases`へスナップショットとして作成する。
- 実行からシナリオを削除できる。

#### 集計

- 合計、完了、残りを表示する。
- 不合格件数がある場合は別表示する。
- ケース状態は以下のとおり。
  - `not_run`：未実行
  - `pass`：合格
  - `fail`：不合格
  - `blocked`：ブロック
  - `skip`：スキップ
- `fail`または`blocked`があると、全ケース入力済み時のシナリオ状態は`completed_fail`になる。
- `skip`は完了として扱うが、合格率の分母には含めない。

### SCR-11 シナリオ実行

シナリオカードまたはテスト項目一覧からシナリオを選択すると実行モードになる。

#### ヘッダー操作

- シナリオ名を含む実行名を表示する。
- 「一時停止」でシナリオを実行中として保存する。
- 「結果を確定」でケース状態から算出した結果を保存する。
- シナリオの担当者、備考、実行日時を入力できる。
- シナリオ状態セレクトは表示専用で、ケース状態から自動算出する。

#### テスト項目一覧サイドバー

- ケースがある場合に表示する。
- 初期状態は閉じている。
- 大画面では左側に固定し、内部スクロールする。
- シナリオごとにケースを並べる。
- ケース状態に応じてチェック、バツ、警告、`S`を表示する。
- ケースをクリックすると本文の該当ケースを開き、スクロールする。

#### ケース結果カード

ケースごとに折りたたみカードを表示する。表示順は以下のとおり。

1. ケースタイトル、現在状態
2. 前提
3. 初期データ詳細
4. タグ
5. 見る場所
6. 手順（操作、期待結果）
7. 備考
8. 証跡
9. 結果詳細
10. 実行日時

#### ケース結果入力

- 結果詳細はラジオ形式で状態を選択する。
- 備考はMarkdownで入力する。
- 実行日時は日時入力または「実行日時を今」で現在時刻を設定する。
- 既存実行の結果、備考、日時は変更時に自動保存する。
- 実績結果（`actual_result`）を保存するDB/API項目はあるが、現行画面に直接の編集欄はない。

### 証跡機能

証跡は用途ごとに以下の3種類を持つ。

| 種類 | 保存対象 | 追加方法 | 現行UI |
| --- | --- | --- | --- |
| シナリオ証跡 | 実行内シナリオ | 複数ファイル選択、画像貼り付け | SCR-11のシナリオ証跡欄 |
| ケース実行証跡 | 実行内ケース | 複数ファイル選択、画像貼り付け | SCR-11のケースカード |
| 見る場所画像 | ケース定義 | 画像選択、画像貼り付け | SCR-04のケース詳細 |

共通動作。

- 添付ファイルはプロジェクトの`attachments/`にコピーする。
- ファイル名は安全化し、UUIDを先頭に付与する。
- 画像は設定に応じて追加時に縮小する。
- 画像はプレビューできる。
- 画像の並び順を上下ボタンまたはドラッグ＆ドロップで変更できる。
- 削除時はDBレコードと保存ファイルを削除する。
- 画像編集前に元画像をバックアップし、「元の画像に戻す」で復元できる。
- 8MBを超える画像は画面内プレビューできず、サイズエラーを表示する。
- 非画像ファイルはメタデータを保持するが、画面内画像プレビューは行わない。

### MOD-02 証跡プレビュー／画像編集

- 画像をcanvasへ読み込む。
- 編集モードはトリミング、ペイント、枠線。
- トリミングはドラッグ選択後「トリミングを適用」で反映する。
- ペイントは色と1～30pxのブラシサイズを指定してドラッグ描画する。
- 枠線は色と1～40pxの太さを指定し、選択範囲へ追加する。
- 選択範囲の枠は編集用表示であり、保存画像には含めない。
- Ctrl+Z／Ctrl+Y、または矢印ボタンでundo／redoする。
- 「元の画像に戻す」は初回編集前の元画像へ戻す。
- 「編集内容を保存」で対象証跡ファイルを更新する。
- 保存時に右上へ一時通知を表示する。
- 編集対象が非画像の場合はプレビュー／編集不可とする。

### SCR-12 エクスポート／インポート

#### エクスポート対象

- テストケース
- シナリオ
- 初期データ
- テスト実行結果

#### 出力形式

- CSV
- JSON
- Markdown

#### 選択条件

| 対象 | 選択 |
| --- | --- |
| テストケース | フォルダ（未分類を含む） |
| シナリオ | シナリオ複数選択 |
| 初期データ | scope選択 |
| テスト実行結果 | 実行複数選択 |

- 「全選択」「解除」を利用できる。
- テスト実行を複数選択した場合、出力先フォルダを選び、実行ごとに別ファイルを作成する。
- それ以外は保存ダイアログで1ファイルを指定する。
- 保存ファイル名は対象名を初期値にする。

#### インポート

- 種類、形式、必要なscopeまたは取り込み先フォルダを選択する。
- ファイル選択またはドラッグ＆ドロップで取り込む。
- 取り込み前に、総件数、最大8列、最大10行をプレビューする。
- テストケースは、ファイル指定のfolderより画面指定の取り込み先フォルダを優先する。
- シナリオは、`case_ids`または`case_titles`で既存ケースに紐付ける。
- 初期データは`scope`、`items`を読み込む。
- テスト実行結果は、シナリオIDまたはタイトル、ケースIDまたはタイトルで既存資産に対応付ける。
- JSONに埋め込まれた画像Data URLは添付ファイルとして復元する。
- 外部ファイルパスだけを持つ証跡はインポートしない。

#### 現行の入出力上の注意

改修時に最優先で整理すべき事項である。

1. アプリのテストケースCSV/Markdown出力は、操作（action）や見る場所画像を完全には出力しないため、完全な往復移行形式ではない。
2. テストケースのエクスポートは期待結果を出力するが、操作と期待結果の対応関係は保持しない形式がある。
3. シナリオのJSON出力は、現行コード上、シナリオレコードではなくシナリオ内ケースの行を返す分岐になっているため、シナリオJSONの往復互換性を満たさない。
4. シナリオの構造化Markdown出力と、インポート側のMarkdown表パーサーは入力形式が一致しない。
5. `import-examples`のサンプル形式は、現行アプリの出力形式と同一とは限らない。サンプルは外部交換フォーマットとして扱う。
6. Web化時は、エクスポート／インポートの正式スキーマをバージョン付きJSONとして定義し、CSV/Markdownは表示・簡易交換用途に限定することを推奨する。

### SCR-13 設定

#### プロジェクト設定

- プロジェクト名を編集できる。
- フォーカスアウト時に保存する。
- 現在のプロジェクトパスを表示する。

#### 外観

- ライト／ダーク切替。
- 文字サイズ14～36px。
- 証跡画像の自動サイズ調整の有効／無効。
- 最大横幅400～4000px。
- 最大縦幅400～4000px。
- 保存済み画像へ現在設定を一括適用する。

#### データベース操作

- バックアップ：指定先フォルダにDBと`attachments/`をコピーする。
- リセット：確認後、現在のDBと添付フォルダを削除し、同じプロジェクト名で空DBを再作成する。
- リセットは不可逆操作として扱う。

## 7. データ仕様

### 7.1 エンティティ一覧

| エンティティ | 主な属性 | 関係 |
| --- | --- | --- |
| `meta` | project name、created_at、updated_at | プロジェクト設定 |
| `test_cases` | title、objective、preconditions、view_location、priority、tags | test_steps、folders、data_sets、scenario、run |
| `test_steps` | position、action、expected | テストケースに所属 |
| `case_folders` | name、parent_id | 自己参照の親子構造 |
| `test_case_folders` | case_id、folder_id | ケースとフォルダの多対多 |
| `scenarios` | title、objective、preconditions | scenario_cases |
| `scenario_cases` | scenario_id、case_id、position | シナリオ内の順序 |
| `data_sets` | name、scope、folder_id、description | data_items、data_links |
| `data_items` | label、value、note、sort_order | データセットに所属 |
| `data_links` | data_set_id、entity_type、entity_id | case/scenario/runとの紐付け |
| `test_runs` | name、environment、build_version、tester、status、notes | run_scenarios |
| `run_scenarios` | scenario_id、title、status、assignee、actual_result、notes、executed_at | 実行内シナリオ |
| `run_scenario_cases` | case_id、status、actual_result、notes、executed_at | 実行内ケース |
| `scenario_evidence` | file_name、stored_path、mime_type、size、sort_order | 実行内シナリオ証跡 |
| `run_case_evidence` | file_name、stored_path、mime_type、size、sort_order | 実行内ケース証跡 |
| `case_view_images` | file_name、stored_path、mime_type、size、sort_order | ケースの見る場所画像 |
| `procedure_documents` | title、source_path、source_hash | Markdown手順書 |
| `procedure_groups` | level、title、path_key、position | 手順書のグループ |
| `procedure_steps` | heading、step_no、position、status、body_text、content_hash | 手順書の手順 |
| `procedure_step_blocks` | block_type、language、content、block_order | 手順本文の構造化ブロック |
| `procedure_schedule` | planned_start_at、planned_end_at、duration、anchor | 手順予定 |
| `procedure_execution` | actual_start_at、actual_end_at、duration | 手順実績 |

### 7.2 状態値

#### テスト実行

```text
draft        下書き
in_progress  実行中
completed    完了
```

#### 実行ケース

```text
not_run  未実行
pass     合格
fail     不合格
blocked  ブロック
skip     スキップ
```

#### 実行シナリオ

```text
not_run         未実行
in_progress     実行中
completed_pass  実行済み（問題なし）
completed_fail  実行済み（不合格またはブロックあり）
```

#### 手順

```text
not_started  未実施
in_progress  実施中
done         完了
```

### 7.3 削除とスナップショット

- 多くの関連テーブルは外部キーの`ON DELETE CASCADE`で親削除に追従する。
- 実行追加時、シナリオ内ケースを`run_scenario_cases`へ作成するため、実行はケース構成のスナップショットを持つ。
- 現行実装では実行画面が元ケースの詳細を再取得して表示するため、ケース定義を後から変更すると過去実行の表示にも影響しうる。
- Web化時は、過去実行を完全な記録として残すため、実行開始時にタイトル、前提、見る場所、手順、期待結果、適用初期データをスナップショット保存することを推奨する。

## 8. 現行制約・リスク・未実装事項

### 8.1 Web化に直接影響する制約

- ログイン、ユーザー、ロール、権限の概念がない。
- 複数ユーザーによる同時編集を想定していない。
- DBは1プロセスが開くローカルSQLiteである。
- ファイル選択、OSクリップボード、OSによるファイルオープンに依存している。
- プロジェクトパスを画面で選択する設計で、サーバー側の共有ストレージ設計がない。
- `localStorage`に表示設定を保存しており、利用者やサーバーDBとは分離されている。
- IPC入力の厳格なスキーマバリデーションは限定的である。
- `index.html`にCSPがなく、Electronのsandboxも明示していない。
- SQLiteと添付ファイルは暗号化されていない。

### 8.2 現行実装で確認できる注意点

- シナリオ前提条件はDB、インポート、エクスポートに存在するが、現行画面では編集できない。
- テスト実行の開始日時・終了日時および実績結果はDB項目があるが、現行の標準入力画面にない。
- 設定画面の「既存画像にもまとめて適用」は、現行コード上、シナリオ証跡と実行ケース証跡が対象で、ケースの見る場所画像は一括処理対象に含まれていない。
- 証跡プレビューは8MB超で表示できない。Web化時はサムネイル生成、ストリーミング、権限制御が必要になる。
- エクスポートの種類ごとに出力構造が異なり、完全な往復互換性がない。
- ファイル名やMarkdownリンクをWeb画面へ移す場合、パス traversal、XSS、危険なURLスキームへの対策が必要である。

### 8.3 セキュリティ上の現状評価

ローカル、オフライン、単一端末という前提では、アプリ自身が外部送信する経路は少なく、外部漏えいリスクは低い。一方で、DBと添付ファイルは平文であり、端末のファイルアクセス権を持つ利用者からは読み取り可能である。Web化では、認証・認可・監査ログ・暗号化・バックアップアクセス制御を必須要件として扱う。

## 9. OpenShift Web化方針（提案）

### 9.1 推奨する論理構成

```text
Browser
  │ HTTPS / OpenShift Route
  ▼
Web UI（React）
  │ HTTPS/JSON API
  ▼
Application API（Node.js/TypeScript）
  ├─ PostgreSQL（定義、結果、監査ログ）
  ├─ Object StorageまたはPVC（証跡、元画像、エクスポート）
  └─ 認証基盤（OpenShift OAuth、OIDC、または社内IdP）
```

### 9.2 現行機能からWeb機能への置換

| 現行 | Web化後の候補 |
| --- | --- |
| プロジェクトフォルダ | ワークスペース／プロジェクトID |
| ローカルSQLite | PostgreSQL |
| `attachments/` | S3互換Object Storage、または要件に応じたPVC |
| Electron IPC | HTTP APIまたはBFF |
| OSファイル選択 | ブラウザのfile upload、ドラッグ＆ドロップ |
| OSクリップボード | Clipboard API、画像貼り付け、ファイルアップロード |
| `shell.openPath` | ブラウザ内プレビューまたは認可付きダウンロード |
| localStorageのテーマ | ユーザー設定。必要に応じてDB保存 |
| DBバックアップ | 管理者操作、定期バックアップジョブ、ストレージ世代管理 |
| DBリセット | 管理者専用、再認証、監査ログ付きのプロジェクト初期化 |

### 9.3 データ移行

移行用の正式フォーマットをJSONで定義する。

- `schema_version`を必須にする。
- project、folders、test_cases、steps、scenarios、data_sets、runs、evidence metadataを分離する。
- IDは移行時に保持できるようにする。
- ケースの操作と期待結果の対応を配列要素単位で保持する。
- シナリオのケース順を保持する。
- 証跡はメタデータとバイナリを別管理し、manifestで参照する。
- 実行開始時のケース定義スナップショットを保存する。
- 既存のSQLiteからJSONへ変換するワンショット移行ツールを用意する。

### 9.4 OpenShift実行要件

- コンテナは非root、任意UIDで起動可能にする。
- アプリは`0.0.0.0`で待ち受け、`PORT`環境変数に対応する。
- readiness probe、liveness probeを実装する。
- DB接続、オブジェクトストレージ接続、マイグレーション失敗を起動ログに明確に出す。
- コンテナイメージ内の書き込み可能領域に業務データを保存しない。
- DBはOpenShift外部のPostgreSQLサービス、または運用管理されたDBを利用する。
- 証跡は容量、保持期間、バックアップ、削除ポリシーを定義する。
- OpenShift RouteはTLSを使用する。
- Secret、ConfigMap、ServiceAccount、ResourceQuota、NetworkPolicyを運用環境に合わせて定義する。
- 複数Podで稼働できるよう、アプリ本体をステートレスにする。
- 長時間のエクスポートや大量画像処理は、必要に応じてジョブ化する。

### 9.5 Web版で追加すべき非機能要件

| 分類 | 初期要件案 |
| --- | --- |
| 認証 | 社内OIDCまたはOpenShift連携 |
| 権限 | 管理者、編集者、実行者、閲覧者 |
| 監査 | 作成、更新、削除、実行結果確定、証跡削除、エクスポートを記録 |
| 同時編集 | 更新日時またはversionによる楽観ロック |
| 可用性 | Pod再起動後もDB・証跡が失われない |
| 性能 | ケース一覧、実行一覧はページング |
| ファイル | サイズ・拡張子・MIME・ウイルススキャン・画像変換 |
| バックアップ | DBと証跡の整合した世代バックアップ |
| ログ | 構造化ログ、request ID、ユーザーID、project ID |
| 監視 | HTTP、DB、ストレージ、ジョブのメトリクス |

## 10. 改修ロードマップ

### Phase 0：現行仕様・データの固定

1. 本仕様書を基準にする。
2. SQLiteから全エンティティと添付ファイルをバックアップする。
3. 既存のCSV/JSON/Markdown入出力の代表例を保存する。
4. 画面ごとの受け入れテストを作成する。
5. 入出力の正式スキーマを決める。

### Phase 1：Web化の基盤

1. React画面とAPIサーバーを分離する。
2. PostgreSQLスキーマとマイグレーションを作成する。
3. 認証・プロジェクト権限・監査ログを追加する。
4. 証跡ストレージの抽象化を追加する。
5. `/healthz`、`/readyz`、構造化ログを追加する。

### Phase 2：定義系機能

1. テストケース一覧・詳細をWeb化する。
2. フォルダツリー、複数フォルダ所属、シナリオ構成を移植する。
3. データセットとMarkdownプレビューを移植する。
4. SQLite移行ツールを実装する。

### Phase 3：実行・証跡

1. テスト実行一覧・詳細・ケース結果を移植する。
2. 実行開始時スナップショットを追加する。
3. ブラウザアップロード、貼り付け、画像編集、プレビューを実装する。
4. 大容量画像のサムネイルとダウンロードを実装する。

### Phase 4：手順書・入出力・運用

1. Markdown手順書のアップロードと再読み込みを移植する。
2. 予定・実績の時間管理を移植する。
3. バージョン付きJSON入出力を正式化する。
4. OpenShiftのDeployment、Service、Route、ConfigMap、Secret、Probeを整備する。
5. 負荷、権限、バックアップ復元、障害復旧を検証する。

## 11. 今後の仕様決定事項

Web版の実装開始前に、以下を決定する。

1. プロジェクトをユーザー、チーム、部署のどの単位で所有するか。
2. 認証方式と、管理者・編集者・実行者・閲覧者の権限範囲。
3. PostgreSQLと証跡ストレージの運用主体、容量、保持期間。
4. 実行結果を定義変更から独立させるためのスナップショット粒度。
5. シナリオ前提条件、実行開始／終了日時、実績結果の入力画面を追加するか。
6. `skip`を合格率に含めるか、別の未評価として扱うか。
7. Markdownの許可記法と、外部リンク・画像の許可範囲。
8. CSV/Markdownを正式交換形式として維持するか、JSONを正式形式に限定するか。
9. 既存のSQLiteプロジェクトをどの単位で移行するか。
10. 削除を物理削除にするか、論理削除と復元機能を追加するか。

