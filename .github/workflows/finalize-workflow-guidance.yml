name: Finalize workflow guidance records

on:
  push:
    branches:
      - agent/folder-explorer-p2
    paths:
      - .github/workflows/finalize-workflow-guidance.yml
      - .github/scripts/finalize_workflow_guidance.py

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/folder-explorer-p2
          fetch-depth: 0

      - name: Update records and README
        run: python .github/scripts/finalize_workflow_guidance.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/ISSUE_LEDGER.md docs/TASK_LOG.md docs/OPEN_ISSUES.md web/README.md
          git diff --cached --check
          git commit -m "Document verified daily workflow improvements"
          git push origin HEAD:agent/folder-explorer-p2
