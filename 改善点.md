次の改善点です。
初期データの初期データ詳細の項目のキーが一文字入力するごとに選択が外れて入力できなくなるからちゃんと入力できるようにして。
テスト実行に関して。画像貼り付けを行ったとき削除ボタンが隠れて押しづらいので修正して。証跡として追加した画像をクリックしたら元画像が見れるようにして。今は中身が見れないので証跡として使えない。
残数の表示が消えてるので、テストの結果を収集して総数、完了、残数を表示させて。
結果詳細で選んだ結果を右上に表示させるのはいいが初期データの数が増えると形が崩れて見栄えが悪くなってるから崩れないようにして。
エクスポートに関して。テストシナリオのエクスポートはタイトルと結果詳細、備考、証跡の画像（あれば）、実行日時を載せるようにして。
デザインに関して。まだデザインが見づらいしわかりにくい。pencil.devというアプリを入れました。UI作成用でclaudeとも連携できます。"\\wsl.localhost\Ubuntu\home\ser\Pencil-linux-x86_64.AppImage"ここにあります。デザイン作成のターンになったら教えてください。このアプリは画面を出してリアルタイムで実行してくれて、こちらから手動で自由にUIを入れ替えたり変更できます。一緒に変更していくので呼んでください。メインのコーディングはcodexにやってもらってください。claudeはプロジェクトの管理とpencil.devを使用したデザインの改修、ｌその他補助をお願いします。終わったらgitの依頼も忘れずに。